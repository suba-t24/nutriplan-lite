steps:
  - name: 'node:20-alpine'
    entrypoint: 'npm'
    args: ['ci']

  - name: 'node:20-alpine'
    entrypoint: 'npm'
    args: ['test']

  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-t',
        'australia-southeast2-docker.pkg.dev/$PROJECT_ID/nutriplan-repo/nutriplan-lite:$COMMIT_SHA',
        '.'
      ]

  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'push',
        'australia-southeast2-docker.pkg.dev/$PROJECT_ID/nutriplan-repo/nutriplan-lite:$COMMIT_SHA'
      ]

  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      [
        'run',
        'deploy',
        'nutriplan-lite',
        '--image',
        'australia-southeast2-docker.pkg.dev/$PROJECT_ID/nutriplan-repo/nutriplan-lite:$COMMIT_SHA',
        '--region',
        'australia-southeast2',
        '--platform',
        'managed',
        '--allow-unauthenticated'
      ]

images:
  - 'australia-southeast2-docker.pkg.dev/$PROJECT_ID/nutriplan-repo/nutriplan-lite:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY